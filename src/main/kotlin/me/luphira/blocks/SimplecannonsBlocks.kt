package me.luphira.blocks

import org.slf4j.LoggerFactory

class SimplecannonsBlocks {
    fun registerBlocks() {
        val logger = LoggerFactory.getLogger("simplecannons")
        logger.info("Simple Cannons will now begin initialising blocks.")
        // To do: Create a function to register blocks to prevent horrors later.
    }
}